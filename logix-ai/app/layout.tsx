import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

export const metadata: Metadata = {
  title: "Logix AI — Turn Empty Miles Into Profitable Miles",
  description:
    "Logix AI is an intelligent logistics marketplace connecting transporters and shippers across India — matching unused truck capacity with cargo to cut empty kilometres and maximise earnings.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={inter.variable}>
      <body className="font-sans">{children}</body>
    </html>
  );
}
